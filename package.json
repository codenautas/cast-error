{
    "name": "errores",
    "description": "typed error (for TypeScript)",
    "version": "0.0.1",
    "repository": "codenautas/errores",
    "files": [
        "../lib"
    ],
    "main": "lib/t-error.js",
    "types": "lib/t-error.ts",
    "scripts": {
        "prepare": "tsc -p src",
        "test": "tsc -p src && tsc -p test && mocha test-bild-local/test"
    },
    "devDependencies": {
        "@types/node": "^16.10.3",
        "@types/mocha": "^9.0.0",
        "mocha": "^9.1.2",
        "typescript": "^4.4.3"
    }
}
